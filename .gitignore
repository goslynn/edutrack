# Secretos y llaves: NUNCA se versionan (ver .env.example como plantilla).
.env
.certs/
*.pem
.quarkus/
.playwright-cli/

# directorio que guarda scripts sql para poblar la base de datos de edutrack 
.seed/
